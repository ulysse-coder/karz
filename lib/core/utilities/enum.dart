
enum UserRole {
  conducteur,
  gardien,
  defaultRole,
}

enum ParkingType {
  private,
  public
}

enum VehiculeType {
  deuxRoues,
  voiture,
  miniBus,
}

enum PlaceStatus {
  reserved,
  occupied,
  free
}

enum ReservationStatus {
  pending,
  validated,
  canceled,
  finished,
}

enum Impressions {
  clean,
  welcoming,
  safety,
  qualityPriceRatio
}