
enum UserRole {
  conducteur,
  gardien,
  defaultRole,
}

enum ParkingType {
  private,
  public,
  defaultType
}

enum VehiculeType {
  deuxRoues,
  voiture,
  miniBus,
  defaultType
}

enum PlaceStatus {
  reserved,
  occupied,
  free
}

enum ReservationStatus {
  pending,
  validated,
  canceled,
  finished,
}

enum Impressions {
  clean,
  welcoming,
  safety,
  qualityPriceRatio
}